j start  # jump to start which is at the back of jal
counter:
addiu $8, $0, 0       # set register 8 to zero
loop:
addiu $4, $4, 2         # add 2 to register 4 for each loop
bne $8, $5, loop
addiu $8, $8, 1         # loop for 2 cycles but ends up being 3 due to it being in the delay slot
jr $31
start:
addu $0, $0, $0
addiu $4, $0, 0   # counter
addiu $5, $0, 2   # number of cycles
jal counter
addu $0, $0, $0
addiu $2, $4, 0  #fill up delay slot
addiu $2, $2, 0x89  # v0 should have 0x89 + 6